apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: certforgeissuers.certforge.io
spec:
  group: certforge.io
  names:
    kind: CertForgeIssuer
    listKind: CertForgeIssuerList
    plural: certforgeissuers
    singular: certforgeissuer
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=='Ready')].status
        - name: URL
          type: string
          jsonPath: .spec.url
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  pattern: '^https://'
                  description: "Base URL of the CertForge server. Use https://app.certgov.app for US East."
                authSecretRef:
                  type: object
                  description: "References a Secret containing a 'token' key with a CertForge API bearer token. Mutually exclusive with workloadIdentity and mtlsSecretRef."
                  required: [name]
                  properties:
                    name:
                      type: string
                workloadIdentity:
                  type: object
                  description: "Configures authentication using a projected ServiceAccount token (OIDC). Mutually exclusive with authSecretRef and mtlsSecretRef."
                  required: [audience]
                  properties:
                    audience:
                      type: string
                      description: "Audience for the projected ServiceAccount token. Must match the CertForge Workload Identity Provider."
                    tokenFile:
                      type: string
                      description: "Path to the projected token file. Defaults to /var/run/secrets/certforge/token."
                mtlsSecretRef:
                  type: object
                  description: "References a Secret written by certforge-issuer-enroll containing mTLS client credentials. When set, uses mTLS cert auth directly (bypasses Cloudflare). Mutually exclusive with authSecretRef and workloadIdentity."
                  required: [name]
                  properties:
                    name:
                      type: string
                issuanceProfileID:
                  type: string
                  description: "Optional default issuance profile ID. Overrides the DTP default. Can be overridden per Certificate via the certforge.io/issuance-profile annotation."
                secretNamespace:
                  type: string
                  description: "Namespace to read the authSecretRef Secret from (CertForgeClusterIssuer only). Defaults to certforge-system."
            status:
              type: object
              properties:
                conditions:
                  type: array
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys: [type]
                  items:
                    type: object
                    required: [type, status]
                    properties:
                      type: {type: string}
                      status: {type: string}
                      reason: {type: string}
                      message: {type: string}
                      lastTransitionTime: {type: string, format: date-time}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: certforgeclusterissuers.certforge.io
spec:
  group: certforge.io
  names:
    kind: CertForgeClusterIssuer
    listKind: CertForgeClusterIssuerList
    plural: certforgeclusterissuers
    singular: certforgeclusterissuer
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=='Ready')].status
        - name: URL
          type: string
          jsonPath: .spec.url
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  pattern: '^https://'
                  description: "Base URL of the CertForge server. Use https://app.certgov.app for US East."
                authSecretRef:
                  type: object
                  description: "References a Secret containing a 'token' key with a CertForge API bearer token. Mutually exclusive with workloadIdentity and mtlsSecretRef."
                  required: [name]
                  properties:
                    name:
                      type: string
                workloadIdentity:
                  type: object
                  description: "Configures authentication using a projected ServiceAccount token (OIDC). Mutually exclusive with authSecretRef and mtlsSecretRef."
                  required: [audience]
                  properties:
                    audience:
                      type: string
                      description: "Audience for the projected ServiceAccount token. Must match the CertForge Workload Identity Provider."
                    tokenFile:
                      type: string
                      description: "Path to the projected token file. Defaults to /var/run/secrets/certforge/token."
                mtlsSecretRef:
                  type: object
                  description: "References a Secret written by certforge-issuer-enroll containing mTLS client credentials. When set, uses mTLS cert auth directly (bypasses Cloudflare). Mutually exclusive with authSecretRef and workloadIdentity."
                  required: [name]
                  properties:
                    name:
                      type: string
                issuanceProfileID:
                  type: string
                  description: "Optional default issuance profile ID. Overrides the DTP default. Can be overridden per Certificate via the certforge.io/issuance-profile annotation."
                secretNamespace:
                  type: string
                  description: "Namespace to read the authSecretRef Secret from. Defaults to certforge-system."
            status:
              type: object
              properties:
                conditions:
                  type: array
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys: [type]
                  items:
                    type: object
                    required: [type, status]
                    properties:
                      type: {type: string}
                      status: {type: string}
                      reason: {type: string}
                      message: {type: string}
                      lastTransitionTime: {type: string, format: date-time}
