apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certgov-k8manual
  namespace: default
spec:
  secretName: certgov-k8manual-tls
  dnsNames:
    - k8manual.certgovernance.app
  issuerRef:
    name: certforge
    kind: CertForgeClusterIssuer
    group: certforge.io
