apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cftest-auto
  namespace: default
spec:
  secretName: cftest-auto-tls
  dnsNames:
    - cftest-auto.${TEST_DOMAIN}
  issuerRef:
    group: certforge.io
    kind: CertForgeClusterIssuer
    name: certforge
