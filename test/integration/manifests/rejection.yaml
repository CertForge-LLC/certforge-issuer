apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cftest-reject
  namespace: default
spec:
  secretName: cftest-reject-tls
  dnsNames:
    - cftest-reject.${TEST_DOMAIN}
  issuerRef:
    group: certforge.io
    kind: CertForgeClusterIssuer
    name: certforge
